docker system prune -a -f

minikube delete --all
minikube start --driver=docker

minikube dashboard
minikube service nginx

Вход в Pod:
kubectl exec -it dep-nginx-7b6d9c64c7-h7vj5 sh
rc-service sshd status

ssh root@(IP)

minikube addons list

 2>/dev/null

         resources:
          limits:
            memory: "128Mi"
            cpu: "500m"


50 20 't'
c 4.5 0.5 7 '*'
c 4.5 0.5 7 '*'

RUN echo "http://dl-2.alpinelinux.org/alpine/edge/community" >> /etc/apk/repositories
sleep infinity

      volumes:
        - name: mysql
          hostPath:
            path: /mnt/mysql
            type: Directory